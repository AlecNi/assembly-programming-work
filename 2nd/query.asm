STKSEG SEGMENT STACK 
    DW 32 DUP(0) 
STKSEG ENDS 

DATASEG SEGMENT 
    LOW_ASCII DB "a"
DATASEG ENDS

ASSUME CS:CODESEG,DS:DATASEG,SS:STKSEG 

CODESEG SEGMENT 
MAIN PROC FAR 
    MOV AX,DATASEG 
    MOV DS,AX 

    MOV CX,26
    MOV BH,[LOW_ASCII]
    MOV AH,2
    
;错误原因：

L:  ;MOV AL,[LOW_ASCII]
    MOV DL,BH
    INC BH
    ;MOV [LOW_ASCII],AL
    INT 21H
    LOOP L

    MOV AX,4C00H 
    INT 21H
    
MAIN ENDP 

CODESEG ENDS 

END MAIN 
substitute
;为什么不能运行？